# Data Pwn - Python Dependencies
# ================================
# Core Requirements (always needed)
# ================================

# HTTP requests & SSL warning suppression
requests>=2.28.0
urllib3>=1.26.0

# SSH brute force (modules/external/services.py)
paramiko>=2.11.0

# ================================
# Optional: Database Connectors
# Install only what you need
# ================================

# MySQL brute force & extraction
mysql-connector-python>=8.0.0

# PostgreSQL brute force & extraction
psycopg2-binary>=2.9.0

# MSSQL brute force (future: modules/extraction/databases.py)
pymssql>=2.2.0

# MongoDB extraction (future: modules/extraction/databases.py)
pymongo>=4.0.0

# Redis extraction (future: modules/extraction/databases.py)
redis>=4.0.0

# ================================
# Development & Testing
# ================================

pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=0.990

# ================================
# Kali Linux System Tools (NOT Python packages)
# Install via: sudo apt install <tool>
# ================================

# nmap        - Network/port mapping
# sqlmap      - SQL injection testing
# hydra       - Brute force attacks
# gobuster    - Directory enumeration
# wpscan      - WordPress scanning
# nikto       - Web vulnerability scanning
# dnsrecon    - DNS enumeration
# whatweb     - Web technology fingerprinting
# sshpass     - SSH password automation
# ldap-utils  - LDAP/AD enumeration (ldapsearch)
# smbclient   - SMB share enumeration
# mysqldump   - MySQL database dumping (mysql-client)